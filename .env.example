# BTC 5m Direction — optional env (Binance uses public API, no keys required)

# Where to write results.jsonl and accuracy.json (default: ./logs)
# LOG_DIR=./logs

# Optional: symbol for Binance USDT-M Futures (default: BTCUSDT)
# BINANCE_SYMBOL=BTCUSDT
